// == Import
import './styles.scss';

// == Composant
const Loader = () => (
  <div className="loader">
    <p className="loader--text">Chargement...</p>
  </div>
);

// == Export
export default Loader;
